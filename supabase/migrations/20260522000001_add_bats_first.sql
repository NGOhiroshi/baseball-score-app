-- =========================================================================
-- 20260522_001: 試合に「先攻/後攻」を追加
--
-- bats_first = TRUE  → 自軍が先攻（表に攻撃）
-- bats_first = FALSE → 自軍が後攻（裏に攻撃）
--
-- 既存試合は先攻(TRUE)をデフォルトとする。
-- =========================================================================

ALTER TABLE games
  ADD COLUMN bats_first BOOLEAN NOT NULL DEFAULT TRUE;
